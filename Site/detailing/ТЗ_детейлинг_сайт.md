# ТЕХНИЧЕСКОЕ ЗАДАНИЕ

## Сайт премиум-детейлинга "APEX DETAILING"

### Цифровой автосалон будущего

---

**Версия документа:** 1.0  
**Дата создания:** 07.12.2025  
**Статус:** Утверждено к разработке

---

## СОДЕРЖАНИЕ

1. [Анализ целевой аудитории и конкурентов](#1-анализ-целевой-аудитории-и-конкурентов)
2. [Визуальная система](#2-визуальная-система)
3. [Анимации и интерактивность](#3-анимации-и-интерактивность)
4. [Структура сайта и User Flow](#4-структура-сайта-и-user-flow)
5. [Технические требования](#5-технические-требования)
6. [Прототипы ключевых экранов](#6-прототипы-ключевых-экранов)
7. [Стратегия тестирования](#7-стратегия-тестирования)
8. [Этапы реализации](#8-этапы-реализации)
9. [Уникальные преимущества дизайна](#9-уникальные-преимущества-дизайна)

---

## 1. АНАЛИЗ ЦЕЛЕВОЙ АУДИТОРИИ И КОНКУРЕНТОВ

### 1.1 Целевая аудитория

#### Первичная аудитория (70% трафика)

**Владельцы премиальных автомобилей (BMW, Mercedes, Audi, Porsche)**

- **Возраст:** 28-55 лет
- **Доход:** от 300 000 ₽/месяц
- **Психографика:** ценят статус, качество, индивидуальный подход
- **Поведение в digital:** используют премиальные приложения, привыкли к безупречному UX
- **Болевые точки:** страх повреждения автомобиля, недоверие к непроверенным мастерским
- **Триггеры покупки:** рекомендации, визуальное подтверждение качества, гарантии

#### Вторичная аудитория (20% трафика)

**Автоэнтузиасты и коллекционеры**

- **Возраст:** 25-45 лет
- **Особенности:** глубоко разбираются в технологиях детейлинга
- **Ожидания:** техническая экспертиза, редкие услуги, премиальные материалы
- **Поведение:** активно изучают портфолио, сравнивают технологии

#### Третичная аудитория (10% трафика)

**Корпоративные клиенты (автопарки, дилерские центры)**

- **Потребности:** оптовые заказы, долгосрочное сотрудничество
- **Критерии выбора:** скорость, объемы, корпоративные условия

### 1.2 Анализ конкурентов

#### Прямой конкурент: vinyl-jam.ru

**Сильные стороны:**

- Структурированный каталог услуг
- Калькулятор стоимости
- Качественная галерея работ
- Мобильная адаптация

**Слабые стороны:**

- Отсутствие 3D-визуализации
- Статичный контент без "вау-эффекта"
- Стандартные анимации
- Нет интерактивного конфигуратора

#### Benchmark-анализ топ-5 детейлинг-сайтов России:

| Критерий                     | vinyl-jam | Конкурент 2 | Конкурент 3 | НАШ ПРОЕКТ |
| ---------------------------- | --------- | ----------- | ----------- | ---------- |
| 3D-модели                    | ❌        | ❌          | ❌          | ✅         |
| Интерактивный калькулятор    | ✅        | ❌          | ✅          | ✅+        |
| Видео до/после               | ❌        | ✅          | ❌          | ✅         |
| Кинематографические анимации | ❌        | ❌          | ❌          | ✅         |
| Конфигуратор пленки          | ❌        | ❌          | ❌          | ✅         |

### 1.3 Уникальное торговое предложение (USP)

> **"Первый детейлинг-центр с полным цифровым погружением — увидьте результат ДО начала работ через 3D-конфигуратор"**

**Ключевые дифференциаторы:**

1. Интерактивная 3D-модель с реальным отображением пленки
2. Кинематографическое представление услуг
3. Персонализированный калькулятор с визуализацией
4. Эффект "цифрового автосалона будущего"

---

## 2. ВИЗУАЛЬНАЯ СИСТЕМА

### 2.1 Философия дизайна

**Концепция: "Liquid Chrome"** — сочетание жидкого металла и неоновых акцентов, создающее ощущение футуристической лаборатории по уходу за автомобилями.

**Три столпа визуальной идентичности:**

1. **Технологичность** — чистые линии, геометрические формы, цифровая эстетика
2. **Премиальность** — глубокие тона, хромированные акценты, продуманные детали
3. **Динамика** — плавные анимации, живые текстуры, интерактивность

### 2.2 Цветовая палитра

#### Основные цвета (Primary)

```css
:root {
	/* Глубокий космос — основной фон */
	--deep-space: #0a0f22;
	--deep-space-gradient: linear-gradient(135deg, #0a0f22 0%, #1a237e 100%);

	/* Полночный индиго — вторичный фон */
	--midnight-indigo: #1a237e;
	--midnight-fade: #12194d;

	/* Электрический синий — основной акцент */
	--electric-blue: #2196f3;
	--electric-glow: rgba(33, 150, 243, 0.4);

	/* Хромированный металлик — детали */
	--chrome-silver: #c0c0c0;
	--chrome-highlight: #e8e8e8;
	--chrome-shadow: #8a8a8a;
}
```

#### Акцентные цвета (Secondary)

```css
:root {
	/* Неоновый циан — hover-эффекты */
	--neon-cyan: #00e5ff;
	--neon-cyan-glow: rgba(0, 229, 255, 0.5);

	/* Предупреждающий янтарь — уведомления */
	--amber-alert: #ffb300;

	/* Успешный изумруд — подтверждения */
	--emerald-success: #00e676;

	/* Платиновый белый — текст */
	--platinum-white: #f5f5f7;
	--platinum-muted: rgba(245, 245, 247, 0.7);
}
```

#### Градиенты

```css
:root {
	/* Hero-секция */
	--hero-gradient: linear-gradient(
		180deg,
		rgba(10, 15, 34, 0.95) 0%,
		rgba(26, 35, 126, 0.85) 50%,
		rgba(10, 15, 34, 0.98) 100%
	);

	/* Карточки услуг */
	--card-gradient: linear-gradient(
		145deg,
		rgba(26, 35, 126, 0.4) 0%,
		rgba(10, 15, 34, 0.8) 100%
	);

	/* CTA-кнопки */
	--cta-gradient: linear-gradient(90deg, #2196f3 0%, #00e5ff 50%, #2196f3 100%);

	/* Хромированная поверхность */
	--chrome-gradient: linear-gradient(
		135deg,
		#e8e8e8 0%,
		#c0c0c0 25%,
		#8a8a8a 50%,
		#c0c0c0 75%,
		#e8e8e8 100%
	);
}
```

#### Психология цветов

| Цвет                    | Эмоциональный посыл                 | Применение      |
| ----------------------- | ----------------------------------- | --------------- |
| Deep Space (#0A0F22)    | Премиальность, глубина, надежность  | Основной фон    |
| Electric Blue (#2196F3) | Технологичность, инновации, доверие | CTA, акценты    |
| Chrome (#C0C0C0)        | Качество, точность, профессионализм | Детали, рамки   |
| Neon Cyan (#00E5FF)     | Энергия, современность, будущее     | Hover, свечение |

### 2.3 Типографика

#### Шрифтовая система

```css
/* Google Fonts Import */
@import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Orbitron:wght@400;500;700&family=Poppins:wght@300;400;500;600;700&display=swap');

:root {
	/* Заголовки — технологичный, мощный */
	--font-display: 'Bebas Neue', sans-serif;

	/* Основной текст — современный, читаемый */
	--font-body: 'Poppins', sans-serif;

	/* Технические данные — цифровой, футуристичный */
	--font-tech: 'Orbitron', sans-serif;
}
```

#### Типографическая шкала

```css
/* Desktop (1440px+) */
.typography-desktop {
	--h1-size: 72px;
	--h1-line-height: 1.1;
	--h1-letter-spacing: 2px;
	--h1-weight: 700;

	--h2-size: 56px;
	--h2-line-height: 1.15;
	--h2-letter-spacing: 1.5px;

	--h3-size: 36px;
	--h3-line-height: 1.2;
	--h3-letter-spacing: 1px;

	--h4-size: 24px;
	--h4-line-height: 1.3;

	--body-size: 18px;
	--body-line-height: 1.6;

	--caption-size: 14px;
	--caption-line-height: 1.5;

	--tech-numbers: 48px; /* Для калькулятора */
	--tech-labels: 12px;
}

/* Tablet (768px - 1439px) */
@media (max-width: 1439px) {
	.typography-tablet {
		--h1-size: 56px;
		--h2-size: 42px;
		--h3-size: 28px;
		--body-size: 16px;
		--tech-numbers: 36px;
	}
}

/* Mobile (< 768px) */
@media (max-width: 767px) {
	.typography-mobile {
		--h1-size: 48px;
		--h2-size: 32px;
		--h3-size: 24px;
		--h4-size: 20px;
		--body-size: 16px;
		--tech-numbers: 32px;
	}
}
```

#### Применение шрифтов

| Элемент           | Шрифт      | Вес | Размер | Трекинг |
| ----------------- | ---------- | --- | ------ | ------- |
| H1 (Hero)         | Bebas Neue | 700 | 72px   | +2px    |
| H2 (Секции)       | Bebas Neue | 700 | 56px   | +1.5px  |
| H3 (Подзаголовки) | Poppins    | 600 | 36px   | 0       |
| Body              | Poppins    | 400 | 18px   | 0       |
| Цены/Числа        | Orbitron   | 500 | 48px   | +1px    |
| Кнопки            | Poppins    | 600 | 16px   | +0.5px  |
| Навигация         | Poppins    | 500 | 14px   | +1px    |

### 2.4 Иконографика

#### Стиль иконок

- **Тип:** Outline с закругленными углами
- **Толщина линии:** 2px
- **Размеры:** 24px (стандарт), 32px (акцент), 48px (feature)
- **Цвет:** Chrome Silver (#C0C0C0), Electric Blue (#2196F3) при hover

#### Кастомные иконки для услуг

```
🛡️ Защитная пленка — щит с волной
🎨 Винил — кисть + рулон
🕶️ Тонировка — стекло с градиентом
✨ Полировка — звезда + капля
🔧 Тюнинг — шестеренка + молния
🧽 Химчистка — капля + спираль
```

### 2.5 Текстуры и материалы

#### Анимированный жидкий металл (фон)

```css
/* SVG-фильтр для эффекта жидкого металла */
.liquid-metal-bg {
	background: var(--deep-space);
	position: relative;
	overflow: hidden;
}

.liquid-metal-bg::before {
	content: '';
	position: absolute;
	top: -50%;
	left: -50%;
	width: 200%;
	height: 200%;
	background: radial-gradient(
			ellipse at 20% 30%,
			rgba(33, 150, 243, 0.15) 0%,
			transparent 50%
		), radial-gradient(
			ellipse at 80% 70%,
			rgba(0, 229, 255, 0.1) 0%,
			transparent 40%
		), radial-gradient(ellipse at 50% 50%, rgba(26, 35, 126, 0.3) 0%, transparent
				60%);
	animation: liquidFlow 20s ease-in-out infinite;
	filter: url(#liquid-distortion);
}

@keyframes liquidFlow {
	0%,
	100% {
		transform: translate(0, 0) rotate(0deg);
	}
	25% {
		transform: translate(-5%, 5%) rotate(2deg);
	}
	50% {
		transform: translate(5%, -5%) rotate(-1deg);
	}
	75% {
		transform: translate(-3%, -3%) rotate(1deg);
	}
}
```

#### Хромированные кнопки

```css
.chrome-button {
	background: var(--chrome-gradient);
	background-size: 200% 100%;
	border: 1px solid rgba(255, 255, 255, 0.2);
	color: var(--deep-space);
	font-family: var(--font-body);
	font-weight: 600;
	padding: 16px 32px;
	border-radius: 4px;
	position: relative;
	overflow: hidden;
	transition: all 0.3s ease;
}

.chrome-button::before {
	content: '';
	position: absolute;
	top: -100%;
	left: -100%;
	width: 300%;
	height: 300%;
	background: linear-gradient(
		45deg,
		transparent 30%,
		rgba(255, 255, 255, 0.5) 50%,
		transparent 70%
	);
	animation: chromeShine 3s ease-in-out infinite;
}

@keyframes chromeShine {
	0% {
		transform: translateX(-100%) rotate(45deg);
	}
	100% {
		transform: translateX(100%) rotate(45deg);
	}
}

.chrome-button:hover {
	background-position: 100% 0;
	box-shadow: 0 0 20px rgba(33, 150, 243, 0.4), 0 0 40px rgba(33, 150, 243, 0.2);
	transform: translateY(-2px);
}
```

#### Неоновые тени для карточек

```css
.service-card {
	background: var(--card-gradient);
	border: 1px solid rgba(33, 150, 243, 0.2);
	border-radius: 12px;
	padding: 32px;
	position: relative;
	transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

.service-card::after {
	content: '';
	position: absolute;
	inset: 0;
	border-radius: 12px;
	box-shadow: 0 0 0 0 rgba(33, 150, 243, 0), 0 20px 40px rgba(0, 0, 0, 0.3);
	transition: all 0.4s ease;
}

.service-card:hover::after {
	box-shadow: 0 0 30px rgba(33, 150, 243, 0.2), 0 0 60px rgba(33, 150, 243, 0.1),
		0 30px 60px rgba(0, 0, 0, 0.4);
}
```

#### Эффект лазерной резки (разделители)

```css
.laser-divider {
	height: 2px;
	background: linear-gradient(
		90deg,
		transparent 0%,
		var(--electric-blue) 20%,
		var(--neon-cyan) 50%,
		var(--electric-blue) 80%,
		transparent 100%
	);
	position: relative;
	opacity: 0;
	transform: scaleX(0);
	transition: all 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

.laser-divider.visible {
	opacity: 1;
	transform: scaleX(1);
}

.laser-divider::before,
.laser-divider::after {
	content: '';
	position: absolute;
	width: 8px;
	height: 8px;
	background: var(--neon-cyan);
	border-radius: 50%;
	top: 50%;
	transform: translateY(-50%);
	box-shadow: 0 0 10px var(--neon-cyan), 0 0 20px var(--neon-cyan);
}

.laser-divider::before {
	left: 0;
}
.laser-divider::after {
	right: 0;
}
```

### 2.6 Сетка и отступы

#### Система сетки

```css
:root {
	/* Контейнер */
	--container-max: 1440px;
	--container-padding: 80px;

	/* Колонки */
	--grid-columns: 12;
	--grid-gap: 32px;

	/* Отступы (8px базовый модуль) */
	--space-xs: 8px;
	--space-sm: 16px;
	--space-md: 24px;
	--space-lg: 32px;
	--space-xl: 48px;
	--space-2xl: 64px;
	--space-3xl: 96px;
	--space-4xl: 128px;

	/* Секции */
	--section-padding: 120px 0;
}

/* Responsive container */
.container {
	max-width: var(--container-max);
	margin: 0 auto;
	padding: 0 var(--container-padding);
}

@media (max-width: 1200px) {
	:root {
		--container-padding: 48px;
	}
}

@media (max-width: 768px) {
	:root {
		--container-padding: 24px;
		--section-padding: 80px 0;
	}
}

@media (max-width: 480px) {
	:root {
		--container-padding: 16px;
	}
}
```

---

## 3. АНИМАЦИИ И ИНТЕРАКТИВНОСТЬ

### 3.1 Философия анимаций

**Принцип: "Purposeful Motion"**
Каждая анимация должна:

1. Направлять внимание пользователя
2. Обеспечивать обратную связь
3. Создавать ощущение премиальности
4. Не мешать взаимодействию

**Библиотека:** GSAP (GreenSock Animation Platform) + ScrollTrigger

### 3.2 Тайминги и easing-функции

```javascript
// Глобальные тайминги
const TIMING = {
	instant: 0.1, // Микро-feedback
	fast: 0.2, // Hover-эффекты
	normal: 0.4, // Стандартные переходы
	slow: 0.6, // Появление элементов
	cinematic: 1.0, // Крупные анимации
	dramatic: 1.5, // Hero-анимации
}

// Easing-функции
const EASING = {
	// Стандартные
	smooth: 'power2.out',
	bounce: 'back.out(1.7)',
	elastic: 'elastic.out(1, 0.3)',

	// Кастомные для премиальности
	luxuryIn: 'power3.in',
	luxuryOut: 'power3.out',
	luxuryInOut: 'power3.inOut',

	// Для технических элементов
	digital: 'steps(12)',
	glitch: 'power4.inOut',
}
```

### 3.3 Hero-секция: анимации

#### Начальная загрузка (Reveal Sequence)

```javascript
// Timeline для Hero-секции
const heroTimeline = gsap.timeline({
	defaults: { ease: EASING.luxuryOut },
})

heroTimeline
	// 1. Фоновое видео появляется
	.fromTo(
		'.hero-video',
		{ opacity: 0, scale: 1.1 },
		{ opacity: 1, scale: 1, duration: TIMING.dramatic }
	)
	// 2. 3D-модель автомобиля "въезжает"
	.fromTo(
		'.car-3d-model',
		{ x: '100%', opacity: 0, rotationY: -30 },
		{ x: 0, opacity: 1, rotationY: 0, duration: TIMING.cinematic },
		'-=0.8'
	)
	// 3. Заголовок печатается
	.fromTo(
		'.hero-title span',
		{ y: 100, opacity: 0 },
		{ y: 0, opacity: 1, duration: TIMING.slow, stagger: 0.05 },
		'-=0.5'
	)
	// 4. Подзаголовок появляется
	.fromTo(
		'.hero-subtitle',
		{ y: 30, opacity: 0 },
		{ y: 0, opacity: 1, duration: TIMING.normal },
		'-=0.3'
	)
	// 5. CTA-кнопка с неоновым свечением
	.fromTo(
		'.hero-cta',
		{ scale: 0.8, opacity: 0 },
		{
			scale: 1,
			opacity: 1,
			duration: TIMING.slow,
			onComplete: () => {
				// Запуск пульсации свечения
				gsap.to('.hero-cta', {
					boxShadow: '0 0 40px rgba(33, 150, 243, 0.6)',
					repeat: -1,
					yoyo: true,
					duration: 1.5,
				})
			},
		},
		'-=0.2'
	)
```

#### Интерактивная 3D-модель

```javascript
// Three.js настройки для 3D-модели автомобиля
const carModelConfig = {
	// Вращение при движении мыши
	mouseFollow: {
		rotationSpeed: 0.001,
		maxRotationX: 15,
		maxRotationY: 30,
		smoothing: 0.05,
	},

	// Зоны для выбора детейлинга
	interactiveZones: [
		{ name: 'hood', label: 'Капот', price: 15000 },
		{ name: 'roof', label: 'Крыша', price: 12000 },
		{ name: 'doors', label: 'Двери', price: 20000 },
		{ name: 'bumpers', label: 'Бамперы', price: 18000 },
		{ name: 'mirrors', label: 'Зеркала', price: 5000 },
		{ name: 'full', label: 'Полная оклейка', price: 85000 },
	],

	// Анимация при выборе зоны
	zoneHighlight: {
		color: '#2196F3',
		emissiveIntensity: 0.5,
		duration: 0.3,
	},
}
```

### 3.4 Скролл-анимации

#### Reveal-анимации секций

```javascript
// ScrollTrigger для появления секций
gsap.utils.toArray('.section').forEach(section => {
	const elements = section.querySelectorAll('.animate-in')

	gsap.fromTo(
		elements,
		{ y: 60, opacity: 0 },
		{
			y: 0,
			opacity: 1,
			duration: TIMING.slow,
			stagger: 0.1,
			ease: EASING.luxuryOut,
			scrollTrigger: {
				trigger: section,
				start: 'top 80%',
				end: 'top 20%',
				toggleActions: 'play none none reverse',
			},
		}
	)
})
```

#### Эффект "капель воды" в галерее

```javascript
// Создание капель при скролле галереи
class WaterDropEffect {
	constructor(container) {
		this.container = container
		this.drops = []
		this.init()
	}

	createDrop(x, y) {
		const drop = document.createElement('div')
		drop.className = 'water-drop'
		drop.style.left = `${x}px`
		drop.style.top = `${y}px`
		this.container.appendChild(drop)

		// Анимация падения
		gsap.to(drop, {
			y: window.innerHeight,
			opacity: 0,
			scale: 0.5,
			duration: 2 + Math.random(),
			ease: 'power1.in',
			onComplete: () => drop.remove(),
		})

		// Блик
		gsap.to(drop, {
			background:
				'linear-gradient(180deg, rgba(255,255,255,0.8) 0%, rgba(33,150,243,0.3) 100%)',
			duration: 0.5,
			yoyo: true,
			repeat: 1,
		})
	}

	init() {
		// Генерация капель при скролле
		ScrollTrigger.create({
			trigger: '.gallery-section',
			start: 'top center',
			end: 'bottom center',
			onUpdate: self => {
				if (Math.random() > 0.95) {
					this.createDrop(Math.random() * window.innerWidth, 0)
				}
			},
		})
	}
}
```

### 3.5 Hover-эффекты

#### Карточки услуг: эффект "до/после"

```css
.service-card {
	perspective: 1000px;
	position: relative;
}

.service-card-inner {
	position: relative;
	width: 100%;
	height: 300px;
	transition: transform 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94);
	transform-style: preserve-3d;
}

.service-card:hover .service-card-inner {
	transform: rotateY(180deg);
}

.service-card-front,
.service-card-back {
	position: absolute;
	width: 100%;
	height: 100%;
	backface-visibility: hidden;
	border-radius: 12px;
	overflow: hidden;
}

.service-card-back {
	transform: rotateY(180deg);
	background: var(--card-gradient);
}
```

#### CTA-кнопка: неоновое свечение

```css
.cta-neon {
	position: relative;
	padding: 18px 48px;
	background: transparent;
	border: 2px solid var(--electric-blue);
	color: var(--platinum-white);
	font-family: var(--font-body);
	font-weight: 600;
	font-size: 16px;
	text-transform: uppercase;
	letter-spacing: 2px;
	cursor: pointer;
	overflow: hidden;
	transition: all 0.3s ease;
}

.cta-neon::before {
	content: '';
	position: absolute;
	inset: 0;
	background: var(--electric-blue);
	transform: scaleX(0);
	transform-origin: right;
	transition: transform 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

.cta-neon:hover::before {
	transform: scaleX(1);
	transform-origin: left;
}

.cta-neon:hover {
	color: var(--deep-space);
	box-shadow: 0 0 20px var(--electric-blue), 0 0 40px rgba(33, 150, 243, 0.5),
		0 0 60px rgba(33, 150, 243, 0.3), inset 0 0 20px rgba(255, 255, 255, 0.1);
}

.cta-neon span {
	position: relative;
	z-index: 1;
}
```

### 3.6 Микроанимации

#### Калькулятор: анимация цифр

```javascript
// Плавное изменение числа в калькуляторе
function animateNumber(element, targetValue, duration = 0.8) {
	const startValue = parseFloat(element.textContent.replace(/\s/g, '')) || 0

	gsap.to(
		{ value: startValue },
		{
			value: targetValue,
			duration: duration,
			ease: 'power2.out',
			onUpdate: function () {
				element.textContent =
					Math.round(this.targets()[0].value).toLocaleString('ru-RU') + ' ₽'
			},
		}
	)
}
```

#### Навигация: неоновая подсветка

```css
.nav-item {
	position: relative;
	padding: 12px 24px;
	color: var(--platinum-muted);
	transition: color 0.3s ease;
}

.nav-item::after {
	content: '';
	position: absolute;
	bottom: 0;
	left: 50%;
	width: 0;
	height: 2px;
	background: var(--electric-blue);
	box-shadow: 0 0 10px var(--electric-blue);
	transform: translateX(-50%);
	transition: width 0.3s ease;
}

.nav-item:hover,
.nav-item.active {
	color: var(--platinum-white);
}

.nav-item:hover::after,
.nav-item.active::after {
	width: 100%;
}

/* Неоновый эффект для активного пункта */
.nav-item.active {
	text-shadow: 0 0 10px var(--electric-blue), 0 0 20px rgba(33, 150, 243, 0.5);
}
```

### 3.7 Звуковой feedback

```javascript
// Аудио-система для взаимодействий
class AudioFeedback {
	constructor() {
		this.sounds = {
			click: new Audio('/sounds/click.mp3'),
			hover: new Audio('/sounds/hover.mp3'),
			success: new Audio('/sounds/success.mp3'),
			slide: new Audio('/sounds/slide.mp3'),
		}

		// Установка громкости (subtle)
		Object.values(this.sounds).forEach(sound => {
			sound.volume = 0.15
		})

		this.enabled = localStorage.getItem('sound') !== 'off'
	}

	play(soundName) {
		if (this.enabled && this.sounds[soundName]) {
			this.sounds[soundName].currentTime = 0
			this.sounds[soundName].play().catch(() => {})
		}
	}

	toggle() {
		this.enabled = !this.enabled
		localStorage.setItem('sound', this.enabled ? 'on' : 'off')
	}
}

// Применение к элементам
const audio = new AudioFeedback()

document.querySelectorAll('.interactive').forEach(el => {
	el.addEventListener('mouseenter', () => audio.play('hover'))
	el.addEventListener('click', () => audio.play('click'))
})
```

---

_Продолжение в файле ТЗ*детейлинг*сайт_часть2.md_
